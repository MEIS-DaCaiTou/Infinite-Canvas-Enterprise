# 客户 2026.09.5 数据升级过渡版

状态：迁移/回退代码、09.5 隔离副本演练和第一跳完整三资产候选验证已完成；第一跳候选版本号为 `2026.09.6`。尚未做真实进程启动和主线安全收敛；不是正式 Release，不得发给客户安装。

## 起点与目的

- 唯一已核对的客户源 Release：`ice-2026.09.5-7609bb1b7cfa`。
- 该版本的更新器只接受 `same-schema-no-migration` 和 `code-release-pointer`，数据库快照 SHA-256 为 `6b4a1deca34b5724217db868d1b8015f222982fddf63e952fb73e8d2966900c4`。
- 第一跳只安装新更新器与迁移原语；不得新增表、登记元数据、修改客户数据库或移动 `data`、`config`、素材目录。
- 第一跳候选由 `codex/customer-095-migration-bridge` 构建。该分支从客户 09.5 发行线分出，尚未与最新 `origin/main` 收敛；完成准确源版本演练之后，仍须单独复核主线安全改动和数据能力，不能直接据此宣布生产发布。
- 后续业务表版本必须另行证明旧数据库的备份、版本登记、迁移、目标健康校验及失败恢复；第一跳仍必须严格保持同 Schema，不在客户库上提前登记版本。

## 已落地的第一跳约束

过渡版 Release 构建器仍生成与 09.5 完全相同的旧数据库证据。新增的 `versioned.py` 是程序代码，不计入旧式 `migration_ids`；否则 09.5 更新器会误判数据库迁移清单变化并拒绝升级。新版更新器识别无版本记录的旧证据时，仅允许目标证据逐字节对应同一 Schema，并只读核验当前数据库的表结构、完整性和外键；任何不符均阻断。它不会在准备或切换中偷偷为旧数据库创建版本表。

## 下一道发布门槛

1. 已在客户 09.5 的准确发布包、源版本指针及有代表性的旧数据库副本上，完成第一跳完整三资产预检、旧更新器准备、指针切换和模拟目标启动失败回退。正式进程的启动、健康和受控停机仍未验证；无需另设干净 Windows 设备门禁，但不能把启动前预检等同于进程验收。
2. 第二跳已新增旧 Schema 登记作业：Release 证据必须同时绑定准确旧 Schema 指纹和只读预演所得的登记后指纹；受控停机后由 SQLite 收敛 WAL，校验无活动连接，生成并核验一致性备份，然后在同一事务中登记版本记录与执行声明的迁移。备份之前不修改业务 Schema。目标版本启动或健康检查失败时，先恢复数据库再恢复版本指针与旧服务。
3. 已注入备份失败、迁移校验失败、目标启动失败、目标健康失败、恢复失败以及提交后结果不明。仅当代码、数据库与运行健康均可证明恢复时报告 `ROLLED_BACK`；恢复不确定时进入 `RECOVERY_REQUIRED`，不自动重试。仍需在正式候选包上验证进程中断后的人工恢复流程。
4. 首个真正改变数据库结构的 Release 必须另列其准确源版本、目标 Schema、迁移清单、业务数据保留断言和恢复证据。不能仅因第一跳成功就发布新增业务表。

## 验证边界

已从 GitHub 正式 `2026.09.5` Release 下载并验证完整三资产（Manifest、Inventory、30 MB 归档），且成功物化。使用该正式 Release 的完整文件复制出两份隔离安装目录，并由发布包内的 Python 与 09.5 初始化器创建旧库，加入模拟用户与画布归属记录，另外创建模拟配置和素材。目标 Release 和启动器使用测试桩，执行真实更新中心的数据库迁移及版本指针写入：成功路径保留数据并登记到版本 2，健康失败路径恢复到无版本记录的旧库和 09.5 指针；两条演练均通过。单元/更新中心定向测试覆盖备份、迁移、回退和失败阻断。测试入口 `ICE_095_RELEASE_ROOT=<已核验的09.5物化目录> py -3.11 -m pytest enterprise/tests/test_customer_095_clone_drill.py -q`。

演练发现正式 09.5 数据库采用 WAL：只读核验可能留下 `-wal/-shm`，最初阻断备份。因此迁移器在已停机的数据库上用 SQLite checkpoint 收敛 WAL；若仍有活动连接或旁路文件，不删除文件、不强行迁移，直接阻断。迁移提交后重新收敛并计算完整主库哈希，防止仅哈希主文件却遗漏 WAL 中的新 Schema。

第一跳隔离候选三资产已由干净提交 `5ea5faeef4c28f956e27c8eeb77428b4bb01c62f` 构建，Release ID 为 `ice-2026.09.6-5ea5faeef4c2`，归档 SHA-256 为 `cc60ea32d20387c2599c866e02ec6b7d150cb3105671189a5e75d4edcb3ea0e3`，Manifest SHA-256 为 `fe9749fa28b683da0e098d615216056cd8d4a0a8f3c9d1f6c0197cae0240a0ba`；本地三资产路径为 `D:\CodeProject\ICE-095-Bridge-Candidate-20260924-5ea5fae`。Release 校验通过，数据库契约与正式 09.5 逐字段相同。

新增可重复的准确源版本演练 `enterprise/tests/test_customer_095_bridge_candidate.py`：在短路径隔离安装副本中，用正式 09.5 自带 Python 和更新器准备、物化、切换；检查旧库中的用户/画布关系、配置及素材保持不变；故障分支模拟目标启动失败后旧更新器恢复 09.5 指针。成功分支使用目标 Release 自带 Python 执行完整便携启动预检，结果为 `pass`。两场景分别通过。它的重启回调为测试桩，不表示真实监听、进程退出/重启和页面可用性已验证。该机现有 LocalAppData 中存在项目目录，正式 launcher 固定调用 Windows Known Folder，因此没有搬移现有目录来执行真实进程链；需用不会触碰现有运行数据的隔离身份或之后的测试设备完成这一项。开发机长临时路径还使旧 09.5 更新器在解包深层运行时文件时出现 Win32 路径长度错误；改用短路径通过。需为客户安装根目录另行核对完整解包路径长度。

当前没有真实客户数据库副本，模拟记录不能替代客户数据验收。`DEFAULT_MIGRATIONS` 仍为空，尚未冻结真实业务表迁移、目标 Schema 或可交付第二跳版本。正式发布前还需真实启动健康检查、主线安全改动收敛以及中断恢复审计；第二跳须单独构建并验证。客户现有 09.5 不受此开发分支影响。

## 第二跳的首个业务数据域

优先选择 [TASK-RECOVERY-1](https://github.com/MEIS-DaCaiTou/Infinite-Canvas-Enterprise/issues/110) 的 Provider 任务执行与对账记录，而非先建部门统计表。主线已有画布任务 JSON 回执和 `user_task_map` 归属映射，但它们尚不能完整表达供应商已受理、本地响应不明、后续查询、费用归属与重启后对账。新表应只存稳定的本地任务身份、用户归属、Provider/模型标识、供应商任务 ID、有限状态、时间戳及结果/费用对账引用；不存 API Key 和完整敏感请求体。表名、字段、索引、约束、版本 2 迁移及失败恢复测试，须在与最新主线收敛并确认首条真实 Provider 查询链路后冻结。

不能现在把 `09.6` 隔离候选的 Release ID 直接写成第二跳的正式源版本：它还没有真实进程启动验收，且与当前 `origin/main` 存在 13 个自动合并冲突（含 Gateway、安全、更新中心和迁移器）。在准确第一跳正式包确定之前生成第二跳三资产，会把未经交付的候选当作客户已安装基线，形成错误的兼容承诺。
