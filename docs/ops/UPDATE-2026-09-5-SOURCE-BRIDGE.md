# 2026.09.5 源版本兼容更新边界

本分支从已公开的 `2026.09.4` 精确提交 `a0d1ccf7c2c3ddb5d90c5dc25aea76d5e13dc65a` 出发，目标是让已安装 `ice-2026.09.4-a0d1ccf7c2c3` 的设备通过原有 Manifest v2 三资产更新器接受一个同数据库结构的新版本。它不是含新业务表的主线功能发布，也不允许借此绕过数据库迁移门禁。

管理后台仅保留一次超级管理员密码确认；下载、Manifest/Inventory/归档校验、同 Schema 检查、不可变 Release 物化、受控停机、指针切换、启动/健康检查与失败回退仍由既有后端执行。进度窗口显示真实作业阶段，不伪造下载百分比。准备阶段完成前须保持页面打开；作业 ID 落盘后可在重新打开页面时续查。

`RECOVERY_REQUIRED` 只表示上一升级结果无法证明一致，后续更新必须阻断。解除前先独立恢复并核对画布、素材、备份及未完成任务；系统再核验当前 Release、完整数据库模式、SQLite 完整性与健康状态，要求人工记录和超级管理员密码，并先写不可变核验文件与安全审计。该动作不修复数据或重新提交 AI 任务。

发布前须从干净提交构建完整 Manifest/Inventory/Windows ZIP，使用与已发布 `2026.09.4` 相同且经证据验证的 CPython Runtime，验证源与目标数据库快照完全一致，并在隔离目录执行真实源→目标升级及目标启动失败回退。未通过这些步骤不得向客户或测试设备宣称可在线升级。稳定 GitHub Release 会被所有已装设备看见；在没有定向更新通道前，测试投放不能被描述为仅对单一设备可见。
