# Enterprise Test Scripts

This folder is the only place for project diagnostic and smoke-test scripts.

Before adding or changing scripts, read:

- `../../PROJECT_HANDOFF_FOR_NEW_AGENT.md`
- `../../AGENT_CONTEXT.md`
- `../../ENTERPRISE_DOCS.md`
- `SMOKE_CHECKLIST.md`

## Scripts

- `diagnose.ps1` checks local version, selected LAN IP, listening ports, proxy settings, and health endpoints.
- `smoke.ps1` runs non-destructive HTTP smoke checks against a running enterprise gateway.
- `test_start_stop.ps1` accepts a temporary runtime root, random ports and the fixed fixture-child wrapper for a non-production lifecycle check. It refuses to kill existing listeners, verifies short-lived CLI start, restart ACK/PID generation changes and owned-only stop/port release.
- `test_stab_1_supervisor_logging.py` verifies merged STAB-1 / PR #78 and the PR #79 detached-host fix with a temporary runtime root, random fixture ports and local HTTP child processes: role-isolated restart, crash-loop blocking, reserved/adopted lock cleanup, instance-bound ACKs, graceful wrapper shutdown, owned-child fallback after Job termination, state atomicity, stream persistence, exact-secret redaction, CLI exit-code contracts, concurrent-stop quiescence, unresolved-listener fail-closed classification, rotation, direct-script stdlib import isolation, no-shell/no-browser runtime boundaries, and the `enterprise/auth`/`PyJWT` manifest contract. Fixture coverage complements the isolated development-device lifecycle smoke; neither is a production rollout conclusion.
- `test_env_1b1a_static_build.py` verifies the merged ENV-1B1A / PR #81 implementation with temporary static trees: full SHA-256 cache parameters; dependency-first CSS `url(...)` / `@import` processing; CSS font/image/root-relative references and cycle/missing/escape failures; transitive font-to-CSS-to-HTML changes; one deterministic HTML build ID for HTML-to-HTML references and cycles; query/fragment preservation; external/data/blob exclusions; URL-encoded Unicode names; deterministic tree/report output independent of path and mtime; source immutability; explicit CLI paths; and the real `index.html -> vendor/css/fonts.css -> vendor/fonts/*.ttf` repository chain staged only under pytest temp. It also verifies isolated `main.py` import/startup behavior and the Git-tracked APP_ROOT audit gate: site fingerprints, operation-count fixtures, W01-W44 anchors (including the ENV-1B2A external Runtime build flow and OPS Release Manifest v2 external build/materialization flow), existing-symbol/script drift, syntax-error fail-closed behavior, stale/unmapped controls and untracked-file exclusion. Static analysis does not prove absence of all unknown writes, and these tests do not validate a formal Release Runtime or full APP_ROOT immutability.
- `test_env_1b1c_runtime_mode.py`, `test_env_1b1c_runtime_manifest.py`, `test_env_1b1c_python_identity.py`, `test_env_1b1c_preflight_contract.py`, `test_env_1b1c_launch_context.py`, `test_env_1b1c_writable_probe.py`, and `test_env_1b1c_release_mismatch.py` cover the ENV-1B1C-B1 Draft pure contract layer using only temporary fixtures: explicit mode parsing; the fixed five-file manifest startup view and hard limits; one shared fail-closed path/reparse inspector; exact Runtime-root-bound and redacted Python identity; forged typed-model rejection; warning-free preflight; exact-canonical launch-context publication with lexical broken-symlink checks and reused-file-ID/content verification; nonce-bound writable-probe cleanup; and ownership-sensitive mismatch decisions. Context publication is not claimed to be standalone CAS: B2 must supply the external exclusive runtime lock. These tests do not import `main` or gateway, listen on ports, create a launcher, or connect controller / host / child lifecycle behavior.
- `test_env_1b1c_b2_portable.py`, `test_env_1b1c_b2_lifecycle_identity.py`, and `test_env_1b1c_b2_windows_wrappers.py` cover the B2 Draft integration with temporary/fake Release roots: one-read pointer hashing, pointer/self/manifest/Python/preflight cross-binding, retained context after pointer changes, existing STAB-1 lock reservation before context publication, state/lock/command identity propagation, runtime-mode/host-style separation, six-field readiness, host/child validation before application import, fixed `-I -B` child commands, formal direct-script launcher grammar and PATH-free wrappers. Correction coverage additionally binds the adopted lock supervisor identity to state and the live process, repeats portable ownership checks before commands and after restart ACK, preserves and rereads post-replace launch context after directory-sync uncertainty without starting a host, and invokes the wrappers through real `cmd.exe` from Unicode/space paths and a different current directory with polluted Python environment variables. The launcher itself fail-closes before environment sanitization, Release layout reads or enterprise imports unless `isolated`, `ignore_environment`, `no_user_site` and both bytecode-policy flags are active. Known Folder tests verify that a forged `LOCALAPPDATA` value is not used as the portable trust root. These tests do not start a real Runtime, use a real bundled Runtime, or establish production validation.
- `test_env_1b2p_runtime_provenance.py` verifies the ENV-1B2P verifier with temporary standard-library fixtures: fixed 36-file core binding and declared `python314._pth` transform; manifest missing/hash/path/schema failures; per-requirement SHA-256 plus nested wheelhouse bidirectional closure (legacy no-hash lock remains parseable but cannot promote dependency true); exact installed distributions with a fixed/reportable `pip`/`setuptools`/`wheel` bootstrap allowlist; independent rebuild-attestation and pip-check schemas; committed Python-source-policy/Git-tree/archive/inventory binding for the official-root true-path; safe ZIP inventory and traversal/ADS/device/duplicate/symlink rejection; independent archive build-record binding to full inventory, candidate tree, dependencies and actual output archive; strict assembled-archive global inventory closure; deterministic path-redacted reports; unchanged inputs; fixed `production_approved=false`; and CLI/output fail-closed boundaries. Fixtures do not rebuild or approve the real Runtime.
- `test_env_1b2a_reproducible_windows_runtime.py` verifies the ENV-1B2A source/lock/wheelhouse/build contracts: official 36-file CPython source policy, exact hashed lock closure, sdist and unsupported-tag rejection, fixed bootstrap policy, owned output roots, path-bearing console-script normalization, deterministic closed archives, deterministic CycloneDX SBOM with a closed Requires-Dist graph, offline/no-shell implementation, official-root reuse of the existing provenance verifier, clean Git HEAD/tree inputs, and an external fixture seam that drives the formal start/status/health/stop Batch wrappers without adding production trust-root overrides. The real Build A/B, pip-check, archive and bundled-Python lifecycle evidence is generated outside Git and summarized in the ENV-1B2A implementation record; tests never package the large artifacts into the repository.
- `test_env_1b1b_path_roots.py`, `test_env_1b1b_current_release.py`, `test_env_1b1b_app_path_integration.py`, and `test_env_1b1b_workflow_overlay.py` verify the ENV-1B1B Draft implementation only with temporary roots: deterministic fourteen-root profiles; non-inheritable factory trust capability required for install/prepare; component containment, same-volume and Windows special-path failure; data-root-anchored DB_PATH with unified default/explicit candidate validation plus parent/file-level/dangling reparse rejection; scoped prepare plus post-create checks; strict pointer parsing/canonical write/foreign residual preservation, pre-replace identity verification, owned-temp fault cleanup, in-process writer serialization, observable write result and replace-after directory sync classification; two-stage portable resolution; isolated FastAPI startup plus canvas/conversation/history/SQLite/upload/preview/OPS/runtime-log root writes without app-root mutation; workflow same-name upload creating user-only overrides while shipped bytes/digest stay unchanged; list/get `builtin` consistency; and shipped/user workflow overlay, copy-on-edit and shipped-only delete refusal. They do not validate a Release activation, a bundled Python, a production deployment, a cross-process activation lock, GitHub CI, Windows handle no-follow semantics, or full APP_ROOT immutability.
- `test_ops_runner.py` verifies the OPS-2A command runner with temporary app roots, including inventory, source-bound SQLite backup manifest/copy, data-check, release validation, upgrade-plan generation, JSONL job logs, portable operation-target anchoring to external STAGING/LOG/BACKUP roots, explicit safe external local targets, and APP_ROOT/release/reparse/source-overlap rejection.
- `test_ops_3a_online_update.py` verifies OPS-3A trusted release metadata and bounded generic provider diagnostics, strict `ops-release-manifest-v1`, repository-bound GitHub Asset API downloads with metadata/asset-specific private-request headers and cross-host credential stripping, atomic download with stable redacted failure detail codes, Windows-safe ZIP traversal/ADS/device-name/control-character/duplicate/ZIP-bomb rejection, fresh staging revalidation, source-tree and compatibility-bound evidence, SQLite-header/sidecar preparation with no database side effects, source-bound backup and data-check evidence, newer-only release selection, redacted structured jobs, non-executing preparation plans, portable workspace anchoring to external STAGING workspace roots, explicit safe external workspace support, APP_ROOT/source-overlap rejection, and direct-runner compatibility using temporary directories only. Fixture connections are explicitly closed and SQLite databases/sidecars must pass immediate same-directory rename and final unlink checks on Windows.
- `test_ops_release_manifest_v2.py` verifies the independent strict detached `ops-release-manifest-v2`: exact Git payload-policy bytes/SHA-256/blob identity, mandatory includes, Git symlink rejection, app-source/global-inventory cross-binding, deterministic closed archive/materialization, shared no-reparse and root-overlap boundaries, Runtime/static/config/database evidence, strict positive minimum-contract fields, per-vendor SBOM/license policy closure, distribution-license fail-closed behavior, and tamper rejection. Real symlink cases remain platform-conditional, with controlled reparse-metadata tests retained so the fail-closed branch is always exercised.
- `test_env_1b3_windows_validation_kit.py` verifies the development-side ENV-1B3 Windows validation kit without starting the Runtime: one W01-W14 matrix and entrypoint, Windows PowerShell 5.1 syntax, locale-independent DateTime/DMTF/null/invalid InstallDate normalization, strict-mode separation of PowerShell success from unset or stale native exit codes, bounded `where.exe` discovery where exit `1` is normal absence, strict-safe mixed uninstall records without `DisplayName`, WindowsApps alias-only clean-baseline classification, recorded BypassNRO deviation, PATH-free fixed-Python boundaries, Windows-safe relative paths, canonical ordinal inventory hashing (including valid zero-length payload files), offline Release archive/inventory closure plus tamper rejection, an actual candidate-04 governance-exception handoff build, a diagnostic-only W01 stabilization Probe whose manifest/SHA256SUMS and clean-Guest fixture are closed, and Candidate 05 final-gate handoff metadata plus explicit UTF-8 evidence-reading instructions. Independent-host Candidate 05 lifecycle, permissions, reboot, resource-interference and final identity cases remain pending execution by the separate Windows test-host Codex.
- `test_sec_1b1_role_auth.py` verifies legacy/new users schemas, including LEGACY `is_admin=NULL` to raw READY `is_admin=0` normalization, explicit role/auth migration planning and rollback, current-state JWT principals, auth-version session invalidation, and existing admin/feature/WebSocket compatibility using temporary SQLite databases only.
- `test_sec_1f0_security_audit.py` verifies the explicit mandatory security-audit migration, append-only triggers, actor validation, caller-owned transactions, fail-closed writes, recursive sensitive-field rejection, and bounded JSON context using temporary SQLite databases only.
- `test_sec_1c0_super_admin_protection.py` verifies the transitional actor-first role matrix, principal auth-version binding, missing-target non-enumeration, raw main.users readback integrity, denied-audit classification, READY legacy-create rejection, post-authorization PBKDF2 execution, username-conflict/create-error contracts, atomic mandatory audit, last-active-super-admin protection, legacy-mutator rejection, and TEMP-users resistance using temporary SQLite databases only.
- `test_sec_1b2_activation_bootstrap.py` verifies main-schema migration qualification, caller-owned SEC-1B1/SEC-1F0 transactions, LEGACY NULL administrator-flag normalization and post-migration READY integrity gates, immutable lifecycle DDL and duplicate-key/type-sensitive bootstrap-audit matching, source-bound formal backups, WAL-to-DELETE preparation with structured external data/schema-change reports, read-only plan validation before any execute prompt, durable commit-state reporting, local-only runner surface and session-impact confirmation, full LEGACY-to-ACTIVE rehearsal, dynamic token invalidation, supported resume states, lock rejection, and rollback injection using temporary SQLite databases only. Backup source/target connections are explicitly closed and all temporary SQLite files/sidecars must be immediately renameable and unlinkable before directory cleanup.
- `ready_user_fixture.py` is test-only SQL setup for normal users in explicit temporary ROLE_AUTH_READY databases; it is not a production creation path.
- `test_ownership_isolation.py` runs non-destructive ownership isolation checks with a temporary SQLite database and temporary project/canvas/conversation files, including A/B/admin project owner, project-list, canvas-move, and direct-ID denial cases.
- `test_smart_canvas_logs.js` verifies Smart Canvas legacy log normalization, merge behavior, and async task completion logging hooks without calling a model provider.
- `SMOKE_CHECKLIST.md` is the manual checklist to run after every upstream update.
- `BROWSER_REGRESSION_CHECKLIST.md` is the browser-level enterprise regression checklist for login, roles, admin console, entry governance, canvas, conversations, assets, and upstream-sync review.
- `browser-regression.md` describes the minimal automation plan for future browser-level regression scripts.
- `UPDATE_TEST_LOG.md` records the actual result after each upstream update test pass.
- `../../ENTERPRISE_ISOLATION_MATRIX.md` is the data-domain and API permission source of truth for Task 3G follow-up isolation tests.
- `../../ENTERPRISE_PERMISSION_DESIGN.md` defines page permissions, administrator switches, phased delivery, and the A/B/admin acceptance contract.
- `../../PROJECT_HANDOFF_FOR_NEW_AGENT.md` is a superseded historical handoff that preserves the PR #18-#24 and Task 3G context. Current navigation and implementation facts come from `../../docs/README.md` and `../../docs/CURRENT_PROJECT_STATUS.md`.

Run scripts from the project root unless a script says otherwise.

Do not place ad-hoc diagnostic or smoke-test scripts in the project root, `static/`, or other upstream-owned folders. Keep them here so upstream updates and Git reviews stay clean.

For every Task 3G implementation PR, add or extend a focused test here before changing access behavior. Use temporary SQLite databases and temporary data files only. The minimum regression roles are normal user A, normal user B, and administrator; cover list filtering, direct-ID denial, resource URLs, refresh/re-login persistence, and WebSocket delivery where applicable.
